<div>
<script>
        const flag = !ready;
        if (a < b && c > d) { go(); }
        let html = "<div class='x'>";
        const not = !!value;
</script>
        <style>
        .box { color: red; }
        .box > p { margin: 0; }
        .a:not(.b) { display: none; }
        </style>
    <p class="!mt-0">{{$value}}</p>
</div>
